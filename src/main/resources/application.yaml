spring:
  application:
    name: ticketing-app

  mongodb:
    uri: mongodb://localhost:27017/ticketing_db

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
    url: /api-docs

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.mongodb.core: DEBUG
    org.mongodb.driver.protocol.command: DEBUG
